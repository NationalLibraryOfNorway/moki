server {
  listen 8087;

  location /moki/ {
    include /etc/nginx/mime.types;
    root /usr/share/nginx/html;
    add_header Cache-Control "public, max-age=1M";
    try_files $uri $uri/ /moki/index.html =404;
  }

  location /moki/papi {
      proxy_pass http://papi-service:8088;
  }

}
